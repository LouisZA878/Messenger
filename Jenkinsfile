pipeline {
    agent any

    stages {
        stage("build"){
            steps {
                echo 'But is it really building? IDk if itll work the app'
            }
        }
        stage("test"){
            steps {
                echo 'testing the app'
            }
        }
        stage("deploy"){
            steps {
                echo 'deploying the app'
            }
        }
    }
}